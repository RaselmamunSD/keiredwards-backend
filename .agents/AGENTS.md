# Workspace Custom Rules

- **Git & Deployment Flow**: Do not deploy changes to the web servers after every code update. Instead, commit and push all modifications to GitHub. Only deploy to the servers when the user explicitly requests to deploy or update the servers.
